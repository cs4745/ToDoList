# 每周待办 v1.1.0

## 🐛 修复（重要）

- **修复便携 exe 的数据 / 开机启动项 / 快捷方式指向临时目录（Temp）的问题**
  electron-builder 的 portable 目标运行时会把程序先解压到系统 Temp 再执行。旧版本误把"进程所在位置"当作 exe 位置，导致：
  - `todos.json` 写在 Temp，系统清理或重启后**数据丢失**；
  - 开机启动项、桌面快捷方式也指向 Temp 路径，重启后**失效**。
- **v1.1.0 改用 electron-builder 注入的原始路径环境变量**（`PORTABLE_EXECUTABLE_DIR` / `PORTABLE_EXECUTABLE_FILE`）。现在数据与开机启动、快捷方式都**正确落在你存放 exe 的原始目录**（如 `E:\ToDolist`）。从 exe 同文件夹双击启动即正常工作，无需关心 Temp。

## ✨ 功能回顾

- 双清单（本周 / 长期）、完成归档、跨周顺延、备注、周切换。
- 无边框置顶窗口、顶部拖动、边缘吸附隐藏 / 滑出、`×` 收托盘。
- 数据随 exe 便携保存（同目录 `todos.json`）；同目录不可写时自动回退到用户数据目录并提示。
- 开机启动、桌面快捷方式。

## 📦 下载与安装

- 下载 `每周待办-1.1.0-便携版.exe`，**双击即用，无需安装**。
- 数据文件 `todos.json` 自动保存在 exe 同目录，移动整个文件夹即可随身带走。
- 建议把整个文件夹放到有权限写入的普通目录（如 `E:\ToDolist`、`D:\Tools\`），不要放 `Program Files`。

## ⚠️ 升级说明

- 用新 exe 覆盖旧 exe 即可；旧版本写在 Temp 的数据已无法找回，新版本从原始目录重新开始。
