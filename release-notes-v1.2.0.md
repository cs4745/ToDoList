# 每周待办 v1.2.0 更新说明

一款极简、置顶、无边框的 Windows 待办小工具，专注「本周要做什么」与「长期要做的事」，数据随 exe 便携保存，解压即用、不写注册表。

## ✨ 本次新增：任务拖拽排序
每条任务**左侧新增 `☰` 拖拽手柄**，按住它即可拖动调整顺序：

- **同列表排序**：「本周」与「长期」两个清单各自可上下拖动排序。
- **跨列表移动**：把任务从「本周」拖到「长期」（或反之），即改变其归属。
- **视觉反馈**：被拖的行半透明，目标位置显示**蓝色插入线**（上沿=插到该条前面，下沿=插到后面）。
- **拖到列表空白 / 空列表**：自动追加到该列表末尾。
- **自动保存**：松手即落盘，下次打开顺序保持不变。
- **不误触**：只有 `☰` 手柄可拖，点文字 / 备注仍是正常编辑。

## 基于 v1.1.0
v1.1.0 已修复便携 exe 运行时把数据 / 开机启动项指向临时目录（Temp）的问题，数据稳定保存在 exe 同目录；本版本在此基础上新增拖拽排序，行为完全一致。

## 📦 升级方式
从 Release 下载 `WeeklyTodo-1.2.0-portable.exe`，**覆盖**原 `每周待办.exe` 即可，无需安装，数据完全兼容。

## 💡 使用提示
- 窗口默认**置顶**；吸附侧边可隐身，鼠标滑过自动展开。
- 点右上角 `×` 不是退出，而是收进**系统托盘**（单击切换显隐，右键「退出」真正关闭）。
- 跨周时已完成任务自动**归档**，未完成任务顺延到下一周。
- 数据文件 `todos.json` 保存在 exe 同目录，移动整个文件夹即可随身带走。
