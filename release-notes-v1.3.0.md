# 每周待办 v1.3.0 更新说明

一款极简、置顶、无边框的 Windows 待办小工具，专注「本周要做什么」与「长期要做的事」，数据随 exe 便携保存，解压即用、不写注册表。

## ✨ 本次新增：一键导出每周工作内容到 HTML
主界面底部新增「导出周报」按钮，一键把当前所有任务导成**自包含单文件 HTML**（内嵌样式，双击即开、无需联网）：

- **三段式周报**：本周任务 / 长期任务 / 已完成归档，结构清晰，直接可贴周会或留存。
- **已完成归档按完成时间倒序**：本周勾掉的任务自动排到最前，一眼看清这周干了啥。
- **保存对话框自选路径**：文件名默认带本周起始日期，可改可换目录；点取消则静默不做任何改动。
- **纯本地生成**：所有内容在本地拼装，不上传任何服务器，可放心外发。

## 基于 v1.2.0
v1.2.0 已支持「任务拖拽排序（☰ 手柄）」；本版本在其基础上新增周报导出，数据文件 `todos.json` 完全兼容，直接覆盖旧 exe 即可。

## 📦 升级方式
从 Release 下载 `WeeklyTodo-1.3.0-portable.exe`，**覆盖**原 exe 即可，无需安装。

## 💡 使用提示
- 导出的 HTML 可用于周会汇报、留档或邮件发送。
- 窗口默认**置顶**；吸附侧边可隐身，鼠标滑过自动展开。
- 点右上角 `×` 收进**系统托盘**（单击切换显隐，右键「退出」真正关闭）。
- 跨周时已完成任务自动**归档**，未完成任务顺延到下一周。
- 数据文件 `todos.json` 保存在 exe 同目录，移动整个文件夹即可随身带走。
