# 每周待办 v1.4.0 更新说明

一款极简、置顶、无边框的 Windows 待办小工具，专注「本周要做什么」与「长期要做的事」，数据随 exe 便携保存，解压即用、不写注册表。

## ✨ 本次新增：软件自动更新
内置更新能力，再也不用手动留意新版本：

- **自动检测**：程序启动后静默检查一次 GitHub 上的最新版本，有更新才会提示，无更新不打扰。
- **自动下载 + 安装**：发现新版本后自动在后台下载，退出程序时自动替换 exe 完成升级，数据文件 `todos.json` 原样保留。
- **两处手动入口**：托盘右键菜单「检查更新」、主界面底部「检查更新」按钮，随时可主动检查。
- **状态可见**：下载进度、就绪提示通过原有 toast 实时反馈；更新就绪后弹窗询问「立即重启 / 稍后」。

## 基于 v1.3.0
v1.3.0 已支持「导出每周工作内容到 HTML」；本版本在其基础上新增应用内自动更新，版本管理彻底自动化。

## 📦 升级方式
从 Release 下载 `WeeklyTodo-1.4.0-portable.exe`，覆盖原 exe 即可。

如果已经是 v1.x 便携版，下次启动会自动提示并一键升级到新版。

## 💡 使用提示
- 自动更新需要联网访问 GitHub；若网络受限，可手动到 Release 页下载新版覆盖。
- 升级只替换 exe，你的任务数据（todos.json）始终跟 exe 放在一起，不会丢。
- 窗口默认置顶；点右上角 × 收进系统托盘（单击切换显隐，右键「退出」真正关闭）。
- 跨周时已完成任务自动归档，未完成任务顺延到下一周。
